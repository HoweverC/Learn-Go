分文件编程（多个源文件）
1.必须放在src目录
2.设置GOPATH环境变量


相同目录
1.同一个目录包名必须一样
2.同一个目录调用别的文件的函数，直接调用即可，无需包名引用


不同目录
1.不同目录包名不一样
2.调用不同包里的函数格式：包名.函数名（）
3.调用别的包的函数，如果函数名是小写无法调用，需要大写

执行顺序
导入一个包
1.执行导入包的init()
2.当前文件的const
3.当前文件的var
4.当前文件的init()
5.main()

go install的使用
1.配置GOBIN
2.执行命令后生成bin(可执行程序)以及pkg（库）
